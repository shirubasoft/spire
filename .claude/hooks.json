{
  "hooks": {
    "PostToolCall": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -qE 'dotnet (add|remove) package|dotnet restore'; then dotnet outdated; fi"
          }
        ]
      },
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'Directory.Packages.props'; then dotnet outdated; fi"
          }
        ]
      }
    ]
  }
}
